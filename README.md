# adapter-finance — EXPERIMENTAL Decision OS execution adapter (finance)

Governed financial tools (payments, transfers, trades) reached only when the
kernel permits. Pairs with `plugin-approval` for human sign-off on large amounts.
**Status: EXPERIMENTAL / INTERFACE-ONLY** — bodies are honest stubs; wire the real
payment/brokerage SDK. Holds no authority.
